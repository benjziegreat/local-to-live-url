{
  "name": "local-to-live-url",
  "version": "1.0.0",
  "private": true,
  "description": "Root scripts to build and run the frontend and backend together.",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "start": "npm run build --prefix frontend && concurrently -n backend,frontend -c blue,green \"npm run start --prefix server\" \"npm run start --prefix frontend\"",
    "stop": "node scripts/stop.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
